#include "main.h"
#include <stdio.h>
/**
 * _abs - compute the ablolute value of an interger
 *
 * @n: number being tested
 iiii
 * Return: always 0
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
